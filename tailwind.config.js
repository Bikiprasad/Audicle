/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./**/*.{ts,tsx}"],
    darkMode: 'class',
    theme: {
        extend: {},
    },
    plugins: [], // add animate plugin later if needed for framer-motion or shadcn
}
